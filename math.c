#include <studio.h>
#include <math.h>
int main(){
    
    int a,b,c;
    printf("Enter your a, b, b and c value./n");
    scanf(%d %d d%, &a, &b, &c)
    
    int power = pow(b, 2);// b**2 b^2
    int product = 4 * a * c;//4ac
    int sub = power - product;// b**2 - 4ac

    double sqaure_root = sqrt(sub); //vb** 2 - 4ac
    double add = (-b + square_root) / (2 * a);
    double sub = (-b + square_root)

    return 0;
}